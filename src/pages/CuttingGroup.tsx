interface CuttingGroupProps extends React.HTMLAttributes<HTMLElement> {}

// eslint-disable-next-line no-empty-pattern
function CuttingGroup({}: CuttingGroupProps) {
  return <div>CuttingGroup</div>
}

export default CuttingGroup
